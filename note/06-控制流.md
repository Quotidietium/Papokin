# 06 · 控制流

> 覆盖：进程启动序列、线程模型、主循环控制、连接接入控制、关停序列、异常/崩溃路径。

## 6.1 进程启动序列（`main.rs:48-189` + `lib.rs:249-358`）

```mermaid
sequenceDiagram
    participant M as main()
    participant Cfg as PumpkinConfig
    participant VD as VanillaData
    participant S as Server::new
    participant PS as PumpkinServer::new
    participant T as Server-Ticker线程
    participant L as PumpkinServer::start

    M->>M: 安装 ring TLS provider (main.rs:54)
    M->>M: rayon 全局池命名 Rayon-Worker-i (:56-59)
    M->>M: set_hook(handle_panic) (:62)
    M->>Cfg: PumpkinConfig::load (读TOML→合并默认→回写→validate) (:70)
    M->>VD: VanillaData::load (banned/ops/whitelist/usercache) (:72)
    M->>M: init_logger (tracing+rustyline+Gzip滚动) (:74)
    M->>M: tokio::spawn(setup_sighandler) (SIGINT/HUP/TERM) (:117)
    M->>S: Server::new(basic,advanced,telemetry,vanilla)
    Note over S: server/mod.rs:160-440 顺序见 04 §4.1<br/>含 level.dat 读/建、维度世界加载、<br/>WorldInit/WorldLoad 插件事件、datapack #minecraft:load
    M->>PS: PumpkinServer::new
    Note over PS: RCON 任务→TCP bind(错误分类退出):281-303<br/>→Query→LAN广播→Ticker 线程 spawn :333-344<br/>→Bedrock UDP/NetherNet 绑定 :346-417
    M->>L: init_plugins (WASM 加载+权限审批+可选热重载 watcher) (:419)
    M->>L: start (:179)
    Note over L: 控制台线程→ServerLoadEvent(Startup)<br/>→start_telemetry→进入 accept 主循环 :479
    L-->>M: 循环退出(SHOULD_STOP)
    M->>M: exit(SERVER_EXIT_CODE) (:188)
```

**启动失败的快速退出点**：TCP 端口占用/权限/不可用（`lib.rs:283-302`，各 `exit(1)`）；Bedrock UDP 绑定失败（`lib.rs:414`）；Ticker 线程 spawn 失败（`lib.rs:340-343`）；level.dat 版本不支持（`server/mod.rs:181-227`）。

## 6.2 线程/执行域模型（全图）

```mermaid
graph TB
    subgraph TOKIO["tokio 多线程 runtime (worker = CPU 核数)"]
        ACC["unified_listener_task<br/>lib.rs:549 四路 select<br/>(Java TCP/Bedrock status/NetherNet/停服)"]
        CONN["每连接任务<br/>登录序列→player 包循环→出站写任务"]
        RCONQ["RCON / Query / LAN / telemetry / 控制台消费"]
        CHIO["每 Level: io_read_work x4 + io_write_work x1<br/>schedule.rs:120-133"]
        WDRV["每插件 WASM Store driver task<br/>concurrent_store.rs:34"]
    end
    subgraph RAYON["rayon 全局池 (Rayon-Worker-i)"]
        WTICK["世界并行 tick / 玩家并行 tick<br/>实体/方块批次 par_chunks / 保存"]
    end
    subgraph CG["ChunkGen-i 专用池 (每 Level, (cpus/2).clamp(2,16))<br/>schedule.rs:135-143"]
        GEN["地形生成各阶段"]
    end
    subgraph OS["专用 OS 线程"]
        TICKER["Server-Ticker<br/>ticker.rs:20 同步游戏循环"]
        SCH["Schedule 调度线程 (每 Level)<br/>schedule.rs:148-177"]
        CONSOLE["控制台 readline (rustyline/stdin)<br/>lib.rs:735,783"]
        JOINER["Thread-Joiner 关服 join level.rs:377-387"]
    end
    ACC --> CONN
    TICKER -->|"runtime.enter()+block_on<br/>触发插件 tick 事件"| TOKIO
    TICKER --> WTICK
    SCH --> CHIO & CG
    CONSOLE -->|"mpsc 通道"| TOKIO
```

**跨池铁律**（`main.rs:44-46` 注释）：从 tokio 调用的 rayon 任务必须非阻塞；耗时结果经通道回传 tokio。

**并发写共享状态的手段**：`ArcSwap`（worlds/命令树/level_info——读多写少整体替换）、`DashMap`（区块表/权限注册表）、`AtomicCell`（Entity 全字段——并行 tick 无锁读写）、`SegQueue`（入站包）、`std Mutex`（小临界区，中毒后 `into_inner` 恢复）。

## 6.3 主循环控制（accept 循环 + tick 循环）

### accept 循环（tokio，`lib.rs:479-486` + `unified_listener_task:549-698`）

`select!` 四分支，循环至 `SHOULD_STOP`：

| 分支 | 动作 |
|------|------|
| Java TCP accept | 设 NODELAY → 分配 client_id（可选 IP 脱敏 `scrub_address`）→ spawn 连接任务：`PendingConnection::handle_login_sequence` → 成功则 `add_player` + `spawn_java_player` → 进入 `progress_player_packets`（keep-alive select 循环）→ 断开清理（保存数据/成就/移除玩家）|
| Bedrock status | `StatusResponder::receive`（UDP ping 应答） |
| NetherNet accept | 建 `BedrockClient` → 登录 → 同 Java 流程 |
| `STOP_INTERRUPT.cancelled()` | 返回 false 退出循环 |

**背压与防御**：EMFILE（fd 耗尽）睡眠 500ms 重试（`lib.rs:633-640`）；其余 accept 错误睡眠 50ms；包限速令牌桶（`packet_limiter.rs`）在登录期与 Play 期双重生效。

### tick 循环（专用线程，`ticker.rs:20-111`）

时序控制：`next_tick += nanoseconds_per_tick`（TPS 可运行时改，`tick_rate_manager.rs:82`）；超前则 `tokio::select!{sleep, cancelled}` 精确补眠；落后则钳制（防死亡螺旋）；sprint 模式 interval=0 连跑；freeze 模式只跑 `tick_players_and_network`（`server/mod.rs:1112`——保持连接活性但不推进模拟）。

## 6.4 连接生命周期控制流（Java，完整）

```mermaid
flowchart TD
    A["accept → PendingConnection::new<br/>状态 HandShake pending.rs:88"] --> B["handle_login_sequence 循环 :205"]
    B --> C{"get_packet 30s 超时? :138"}
    C -->|超时| KICK["kick & Stop"]
    C -->|包| D{"packet_limiter :207"}
    D -->|超限| KICK
    D -->|通过| E{"connection_state"}
    E -->|HandShake| F["next_state 路由 :261"]
    E -->|Status| G["ping → 关闭 status.rs"]
    E -->|Login/Transfer| H["login_start→加密→认证<br/>见 05 §5.5"]
    E -->|Config| I["client_information/资源包/插件消息<br/>→ CFinishConfiguration"]
    H --> J["ReadyToPlay(profile,config)"]
    I --> J
    J --> L["JavaClient::from_pending<br/>start_outgoing_packet_task lib.rs:592-593"]
    L --> M["add_player → spawn_java_player<br/>进度包/初始化 :596-605"]
    M --> N["progress_player_packets 主循环<br/>net/java/mod.rs:275"]
    N --> O["select: keep-alive 计时<br/>| close_token | 入站包入队"]
    O --> P["Player::tick 消费包+模拟 :2564"]
    P --> Q{"断开? 超时? kick?"}
    Q -->|否| O
    Q -->|是| R["close → await_tasks<br/>player.remove → remove_player"]
    R --> S["保存玩家数据/成就 lib.rs:615-625"]
```

## 6.5 关停序列（正常停服）

```mermaid
sequenceDiagram
    participant Sig as 信号/Ctrl-C/stop命令
    participant Main as PumpkinServer::start
    participant Tick as Ticker
    participant S as Server
    Sig->>Main: stop_or_exit_server (lib.rs:219)
    Note over Main: 二次调用直接 exit(强制)
    Main->>Main: SHOULD_STOP=true + STOP_INTERRUPT.cancel()
    Tick-->>Main: select 感知 cancelled 退出 (:80)
    Main-->>Main: accept 循环退出 SERVER_IS_STOPPING=true (:488)
    alt 有崩溃报告
        Main->>Main: 打印/保存 CrashReport, EXIT_CODE=1 (:490-502)
    end
    Main->>S: save_all_players 玩家数据 (:506)
    Main->>S: advancement save_all (:514)
    Main->>Main: 踢出全部玩家 "Server stopped" (:523)
    Main->>Main: tasks.close+wait 连接任务排空 (:530)
    Main->>S: unload_plugins → on_unload + Store shutdown (:533)
    Main->>S: server.shutdown (:537)
    Note over S: server/mod.rs:747-767<br/>TaskTracker 收尾→各 world.shutdown<br/>(chunk 线程 3s join 超时 level.rs:377)<br/>→写 level.dat
    Main-->>Main: exit(SERVER_EXIT_CODE)
```

## 6.6 异常与崩溃控制流

### panic 处理（`main.rs:233-305`）

```mermaid
flowchart TD
    P["任意线程 panic"] --> Q{"是主线程?"}
    Q -->|是| R["CRASH_REPORT OnceLock 抢占<br/>首次→打印+保存→exit(1)"]
    Q -->|否| T{"首次 panic?"}
    T -->|是| U["生成 CrashReport<br/>stop_server 优雅关停"]
    T -->|后续| V["仅 error 日志<br/>(关停中再崩不递归)"]
```

### 错误分级（`error.rs:9-19`）

`PumpkinError` trait：`is_kick()`（该错误是否应踢玩家）/ `severity()` / `client_kick_reason()`。包处理错误在 `process_inbound_packets`（`player.rs:2522-2540`）按此分级：可踢错误 → `try_kick(原因)`；否则记录 error 日志继续。

### 其他防御点

| 机制 | 位置 |
|------|------|
| 登录 30s 空闲超时（防慢速占用 fd） | `pending.rs:47-55` |
| keep-alive 双向超时踢出 | `net/java/mod.rs:289-311` |
| 包速率令牌桶（登录期+游戏期） | `packet_limiter.rs`，`pending.rs:207` |
| 区块生成非阻塞约束（防 tokio 饿死） | `main.rs:44-46` |
| WASM 重入深度上限 64（防插件递归死锁） | `plugin-runtime chain.rs:12,117-129` |
| WASM 内存上限/沙箱目录/socket 策略 | `wasm_host/mod.rs:396-423,49-110` |
| 关停中二次中断 → 立即 exit | `lib.rs:219-225` |
| unix fd 上限自动提升（上限 1M，回退 65k） | `lib.rs:853-905` |

## 6.7 控制流要点总结

1. **双循环结构**：tokio 上的 accept/IO 循环 + 专用线程上的同步 tick 循环；两者通过 `Player.inbound_packets` 队列与 `Server.runtime` 句柄（`runtime.enter()`）桥接。
2. **停服是全局协作**：单一 `CancellationToken`（`STOP_INTERRUPT`）同时唤醒 accept 循环、tick 睡眠、控制台、各后台任务；`SERVER_IS_STOPPING` 防重入，`CRASH_REPORT` 区分优雅/崩溃退出码。
3. **tick 内的一切皆可并行**：世界间、玩家间、实体批、方块计划 tick、刷怪、保存 —— rayon 分批；但**插件 blocking 事件在 tick 线程 `block_on` 同步等待**，保证事件时序确定性。
4. **冻结/冲刺是一等状态**：`ServerTickRateManager`（`tick_rate_manager.rs:30-55`）让 `/tick freeze|step|sprint|rate` 直接改变循环节奏而不破坏 50ms 基准。
